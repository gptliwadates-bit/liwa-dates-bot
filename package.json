{
  "name": "liwa-dates-ai-agent",
  "version": "1.0.0",
  "description": "وكيل رد آلي بالذكاء الاصطناعي لتمور ليوا عبر Meta (فيسبوك/انستجرام/واتساب) + Claude",
  "main": "webhook-server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node webhook-server.js",
    "test": "node --test",
    "lint": "node -e \"const fs=require('fs');let ok=true;for(const d of ['lib','prompts','test']){for(const f of fs.readdirSync(d)){if(f.endsWith('.js')){try{require('child_process').execSync('node --check '+d+'/'+f)}catch(e){ok=false;console.error('lint fail',d+'/'+f)}}}}require('child_process').execSync('node --check webhook-server.js');require('child_process').execSync('node --check api/index.js');console.log(ok?'lint ok':'lint failed');process.exit(ok?0:1)\"",
    "build": "echo \"no build step (serverless) — Vercel serves api/index.js\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
